/*
 * Ledger Adapter Specification
 * This is the API specification for the Ledger Adapter with whom the FinP2P Router will interact in order to execute and query the underlying implementation.
 *
 * The version of the OpenAPI document: x.x.x
 * Contact: support@ownera.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package io.ownera.ledger.adapter.api.model;

import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.StringJoiner;
import java.util.Objects;
import java.util.Map;
import java.util.HashMap;
import java.util.Locale;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonValue;
import io.ownera.ledger.adapter.api.model.IbanAccountDetails;
import io.ownera.ledger.adapter.api.model.SortCodeDetails;
import io.ownera.ledger.adapter.api.model.SwiftAccountDetails;
import java.util.Arrays;
import com.fasterxml.jackson.annotation.JsonPropertyOrder;

import com.fasterxml.jackson.core.type.TypeReference;

import java.io.IOException;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.Locale;

import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.JsonToken;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonMappingException;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.MapperFeature;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import io.ownera.ledger.adapter.api.ApiClient;
import io.ownera.ledger.adapter.api.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2025-10-27T09:27:05.154160+02:00[Asia/Jerusalem]", comments = "Generator version: 7.16.0")
@JsonDeserialize(using = WireDetails.WireDetailsDeserializer.class)
@JsonSerialize(using = WireDetails.WireDetailsSerializer.class)
public class WireDetails extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(WireDetails.class.getName());

    public static class WireDetailsSerializer extends StdSerializer<WireDetails> {
        public WireDetailsSerializer(Class<WireDetails> t) {
            super(t);
        }

        public WireDetailsSerializer() {
            this(null);
        }

        @Override
        public void serialize(WireDetails value, JsonGenerator jgen, SerializerProvider provider) throws IOException, JsonProcessingException {
            jgen.writeObject(value.getActualInstance());
        }
    }

    public static class WireDetailsDeserializer extends StdDeserializer<WireDetails> {
        public WireDetailsDeserializer() {
            this(WireDetails.class);
        }

        public WireDetailsDeserializer(Class<?> vc) {
            super(vc);
        }

        @Override
        public WireDetails deserialize(JsonParser jp, DeserializationContext ctxt) throws IOException, JsonProcessingException {
            JsonNode tree = jp.readValueAsTree();
            Object deserialized = null;
            boolean typeCoercion = ctxt.isEnabled(MapperFeature.ALLOW_COERCION_OF_SCALARS);
            int match = 0;
            JsonToken token = tree.traverse(jp.getCodec()).nextToken();
            // deserialize IbanAccountDetails
            try {
                boolean attemptParsing = true;
                // ensure that we respect type coercion as set on the client ObjectMapper
                if (IbanAccountDetails.class.equals(Integer.class) || IbanAccountDetails.class.equals(Long.class) || IbanAccountDetails.class.equals(Float.class) || IbanAccountDetails.class.equals(Double.class) || IbanAccountDetails.class.equals(Boolean.class) || IbanAccountDetails.class.equals(String.class)) {
                    attemptParsing = typeCoercion;
                    if (!attemptParsing) {
                        attemptParsing |= ((IbanAccountDetails.class.equals(Integer.class) || IbanAccountDetails.class.equals(Long.class)) && token == JsonToken.VALUE_NUMBER_INT);
                        attemptParsing |= ((IbanAccountDetails.class.equals(Float.class) || IbanAccountDetails.class.equals(Double.class)) && token == JsonToken.VALUE_NUMBER_FLOAT);
                        attemptParsing |= (IbanAccountDetails.class.equals(Boolean.class) && (token == JsonToken.VALUE_FALSE || token == JsonToken.VALUE_TRUE));
                        attemptParsing |= (IbanAccountDetails.class.equals(String.class) && token == JsonToken.VALUE_STRING);
                    }
                }
                if (attemptParsing) {
                    deserialized = tree.traverse(jp.getCodec()).readValueAs(IbanAccountDetails.class);
                    // TODO: there is no validation against JSON schema constraints
                    // (min, max, enum, pattern...), this does not perform a strict JSON
                    // validation, which means the 'match' count may be higher than it should be.
                    match++;
                    log.log(Level.FINER, "Input data matches schema 'IbanAccountDetails'");
                }
            } catch (Exception e) {
                // deserialization failed, continue
                log.log(Level.FINER, "Input data does not match schema 'IbanAccountDetails'", e);
            }

            // deserialize SortCodeDetails
            try {
                boolean attemptParsing = true;
                // ensure that we respect type coercion as set on the client ObjectMapper
                if (SortCodeDetails.class.equals(Integer.class) || SortCodeDetails.class.equals(Long.class) || SortCodeDetails.class.equals(Float.class) || SortCodeDetails.class.equals(Double.class) || SortCodeDetails.class.equals(Boolean.class) || SortCodeDetails.class.equals(String.class)) {
                    attemptParsing = typeCoercion;
                    if (!attemptParsing) {
                        attemptParsing |= ((SortCodeDetails.class.equals(Integer.class) || SortCodeDetails.class.equals(Long.class)) && token == JsonToken.VALUE_NUMBER_INT);
                        attemptParsing |= ((SortCodeDetails.class.equals(Float.class) || SortCodeDetails.class.equals(Double.class)) && token == JsonToken.VALUE_NUMBER_FLOAT);
                        attemptParsing |= (SortCodeDetails.class.equals(Boolean.class) && (token == JsonToken.VALUE_FALSE || token == JsonToken.VALUE_TRUE));
                        attemptParsing |= (SortCodeDetails.class.equals(String.class) && token == JsonToken.VALUE_STRING);
                    }
                }
                if (attemptParsing) {
                    deserialized = tree.traverse(jp.getCodec()).readValueAs(SortCodeDetails.class);
                    // TODO: there is no validation against JSON schema constraints
                    // (min, max, enum, pattern...), this does not perform a strict JSON
                    // validation, which means the 'match' count may be higher than it should be.
                    match++;
                    log.log(Level.FINER, "Input data matches schema 'SortCodeDetails'");
                }
            } catch (Exception e) {
                // deserialization failed, continue
                log.log(Level.FINER, "Input data does not match schema 'SortCodeDetails'", e);
            }

            // deserialize SwiftAccountDetails
            try {
                boolean attemptParsing = true;
                // ensure that we respect type coercion as set on the client ObjectMapper
                if (SwiftAccountDetails.class.equals(Integer.class) || SwiftAccountDetails.class.equals(Long.class) || SwiftAccountDetails.class.equals(Float.class) || SwiftAccountDetails.class.equals(Double.class) || SwiftAccountDetails.class.equals(Boolean.class) || SwiftAccountDetails.class.equals(String.class)) {
                    attemptParsing = typeCoercion;
                    if (!attemptParsing) {
                        attemptParsing |= ((SwiftAccountDetails.class.equals(Integer.class) || SwiftAccountDetails.class.equals(Long.class)) && token == JsonToken.VALUE_NUMBER_INT);
                        attemptParsing |= ((SwiftAccountDetails.class.equals(Float.class) || SwiftAccountDetails.class.equals(Double.class)) && token == JsonToken.VALUE_NUMBER_FLOAT);
                        attemptParsing |= (SwiftAccountDetails.class.equals(Boolean.class) && (token == JsonToken.VALUE_FALSE || token == JsonToken.VALUE_TRUE));
                        attemptParsing |= (SwiftAccountDetails.class.equals(String.class) && token == JsonToken.VALUE_STRING);
                    }
                }
                if (attemptParsing) {
                    deserialized = tree.traverse(jp.getCodec()).readValueAs(SwiftAccountDetails.class);
                    // TODO: there is no validation against JSON schema constraints
                    // (min, max, enum, pattern...), this does not perform a strict JSON
                    // validation, which means the 'match' count may be higher than it should be.
                    match++;
                    log.log(Level.FINER, "Input data matches schema 'SwiftAccountDetails'");
                }
            } catch (Exception e) {
                // deserialization failed, continue
                log.log(Level.FINER, "Input data does not match schema 'SwiftAccountDetails'", e);
            }

            if (match == 1) {
                WireDetails ret = new WireDetails();
                ret.setActualInstance(deserialized);
                return ret;
            }
            throw new IOException(String.format(Locale.ROOT, "Failed deserialization for WireDetails: %d classes match result, expected 1", match));
        }

        /**
         * Handle deserialization of the 'null' value.
         */
        @Override
        public WireDetails getNullValue(DeserializationContext ctxt) throws JsonMappingException {
            throw new JsonMappingException(ctxt.getParser(), "WireDetails cannot be null");
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<>();

    public WireDetails() {
        super("oneOf", Boolean.FALSE);
    }

    public WireDetails(IbanAccountDetails o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    public WireDetails(SortCodeDetails o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    public WireDetails(SwiftAccountDetails o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("IbanAccountDetails", IbanAccountDetails.class);
        schemas.put("SortCodeDetails", SortCodeDetails.class);
        schemas.put("SwiftAccountDetails", SwiftAccountDetails.class);
        JSON.registerDescendants(WireDetails.class, Collections.unmodifiableMap(schemas));
        // Initialize and register the discriminator mappings.
        Map<String, Class<?>> mappings = new HashMap<String, Class<?>>();
        mappings.put("iban", IbanAccountDetails.class);
        mappings.put("sortCode", SortCodeDetails.class);
        mappings.put("swift", SwiftAccountDetails.class);
        mappings.put("wireDetails", WireDetails.class);
        JSON.registerDiscriminator(WireDetails.class, "type", mappings);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return WireDetails.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * IbanAccountDetails, SortCodeDetails, SwiftAccountDetails
     *
     * It could be an instance of the 'oneOf' schemas.
     * The oneOf child schemas may themselves be a composed schema (allOf, anyOf, oneOf).
     */
    @Override
    public void setActualInstance(Object instance) {
        if (JSON.isInstanceOf(IbanAccountDetails.class, instance, new HashSet<Class<?>>())) {
            super.setActualInstance(instance);
            return;
        }

        if (JSON.isInstanceOf(SortCodeDetails.class, instance, new HashSet<Class<?>>())) {
            super.setActualInstance(instance);
            return;
        }

        if (JSON.isInstanceOf(SwiftAccountDetails.class, instance, new HashSet<Class<?>>())) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be IbanAccountDetails, SortCodeDetails, SwiftAccountDetails");
    }

    /**
     * Get the actual instance, which can be the following:
     * IbanAccountDetails, SortCodeDetails, SwiftAccountDetails
     *
     * @return The actual instance (IbanAccountDetails, SortCodeDetails, SwiftAccountDetails)
     */
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `IbanAccountDetails`. If the actual instance is not `IbanAccountDetails`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `IbanAccountDetails`
     * @throws ClassCastException if the instance is not `IbanAccountDetails`
     */
    public IbanAccountDetails getIbanAccountDetails() throws ClassCastException {
        return (IbanAccountDetails)super.getActualInstance();
    }

    /**
     * Get the actual instance of `SortCodeDetails`. If the actual instance is not `SortCodeDetails`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `SortCodeDetails`
     * @throws ClassCastException if the instance is not `SortCodeDetails`
     */
    public SortCodeDetails getSortCodeDetails() throws ClassCastException {
        return (SortCodeDetails)super.getActualInstance();
    }

    /**
     * Get the actual instance of `SwiftAccountDetails`. If the actual instance is not `SwiftAccountDetails`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `SwiftAccountDetails`
     * @throws ClassCastException if the instance is not `SwiftAccountDetails`
     */
    public SwiftAccountDetails getSwiftAccountDetails() throws ClassCastException {
        return (SwiftAccountDetails)super.getActualInstance();
    }



  /**
   * Convert the instance into URL query string.
   *
   * @return URL query string
   */
  public String toUrlQueryString() {
    return toUrlQueryString(null);
  }

  /**
   * Convert the instance into URL query string.
   *
   * @param prefix prefix of the query string
   * @return URL query string
   */
  public String toUrlQueryString(String prefix) {
    String suffix = "";
    String containerSuffix = "";
    String containerPrefix = "";
    if (prefix == null) {
      // style=form, explode=true, e.g. /pet?name=cat&type=manx
      prefix = "";
    } else {
      // deepObject style e.g. /pet?id[name]=cat&id[type]=manx
      prefix = prefix + "[";
      suffix = "]";
      containerSuffix = "]";
      containerPrefix = "[";
    }

    StringJoiner joiner = new StringJoiner("&");

    if (getActualInstance() instanceof IbanAccountDetails) {
        if (getActualInstance() != null) {
          joiner.add(((IbanAccountDetails)getActualInstance()).toUrlQueryString(prefix + "one_of_0" + suffix));
        }
        return joiner.toString();
    }
    if (getActualInstance() instanceof SwiftAccountDetails) {
        if (getActualInstance() != null) {
          joiner.add(((SwiftAccountDetails)getActualInstance()).toUrlQueryString(prefix + "one_of_1" + suffix));
        }
        return joiner.toString();
    }
    if (getActualInstance() instanceof SortCodeDetails) {
        if (getActualInstance() != null) {
          joiner.add(((SortCodeDetails)getActualInstance()).toUrlQueryString(prefix + "one_of_2" + suffix));
        }
        return joiner.toString();
    }
    return null;
  }

}

