/*
 * Ledger Adapter Specification
 * This is the API specification for the Ledger Adapter with whom the FinP2P Router will interact in order to execute and query the underlying implementation.
 *
 * The version of the OpenAPI document: x.x.x
 * Contact: support@ownera.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package io.ownera.ledger.adapter.api.model;

import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.StringJoiner;
import java.util.Objects;
import java.util.Map;
import java.util.HashMap;
import java.util.Locale;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonValue;
import io.ownera.ledger.adapter.api.model.BalanceMarkerTimestamp;
import io.ownera.ledger.adapter.api.model.BalanceMarkerTransactionBlock;
import java.util.Arrays;
import com.fasterxml.jackson.annotation.JsonPropertyOrder;

import com.fasterxml.jackson.core.type.TypeReference;

import java.io.IOException;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.Locale;

import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.JsonToken;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonMappingException;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.MapperFeature;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import io.ownera.ledger.adapter.api.ApiClient;
import io.ownera.ledger.adapter.api.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2025-10-27T09:27:05.154160+02:00[Asia/Jerusalem]", comments = "Generator version: 7.16.0")
@JsonDeserialize(using = BalanceMarker.BalanceMarkerDeserializer.class)
@JsonSerialize(using = BalanceMarker.BalanceMarkerSerializer.class)
public class BalanceMarker extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(BalanceMarker.class.getName());

    public static class BalanceMarkerSerializer extends StdSerializer<BalanceMarker> {
        public BalanceMarkerSerializer(Class<BalanceMarker> t) {
            super(t);
        }

        public BalanceMarkerSerializer() {
            this(null);
        }

        @Override
        public void serialize(BalanceMarker value, JsonGenerator jgen, SerializerProvider provider) throws IOException, JsonProcessingException {
            jgen.writeObject(value.getActualInstance());
        }
    }

    public static class BalanceMarkerDeserializer extends StdDeserializer<BalanceMarker> {
        public BalanceMarkerDeserializer() {
            this(BalanceMarker.class);
        }

        public BalanceMarkerDeserializer(Class<?> vc) {
            super(vc);
        }

        @Override
        public BalanceMarker deserialize(JsonParser jp, DeserializationContext ctxt) throws IOException, JsonProcessingException {
            JsonNode tree = jp.readValueAsTree();
            Object deserialized = null;
            boolean typeCoercion = ctxt.isEnabled(MapperFeature.ALLOW_COERCION_OF_SCALARS);
            int match = 0;
            JsonToken token = tree.traverse(jp.getCodec()).nextToken();
            // deserialize BalanceMarkerTimestamp
            try {
                boolean attemptParsing = true;
                // ensure that we respect type coercion as set on the client ObjectMapper
                if (BalanceMarkerTimestamp.class.equals(Integer.class) || BalanceMarkerTimestamp.class.equals(Long.class) || BalanceMarkerTimestamp.class.equals(Float.class) || BalanceMarkerTimestamp.class.equals(Double.class) || BalanceMarkerTimestamp.class.equals(Boolean.class) || BalanceMarkerTimestamp.class.equals(String.class)) {
                    attemptParsing = typeCoercion;
                    if (!attemptParsing) {
                        attemptParsing |= ((BalanceMarkerTimestamp.class.equals(Integer.class) || BalanceMarkerTimestamp.class.equals(Long.class)) && token == JsonToken.VALUE_NUMBER_INT);
                        attemptParsing |= ((BalanceMarkerTimestamp.class.equals(Float.class) || BalanceMarkerTimestamp.class.equals(Double.class)) && token == JsonToken.VALUE_NUMBER_FLOAT);
                        attemptParsing |= (BalanceMarkerTimestamp.class.equals(Boolean.class) && (token == JsonToken.VALUE_FALSE || token == JsonToken.VALUE_TRUE));
                        attemptParsing |= (BalanceMarkerTimestamp.class.equals(String.class) && token == JsonToken.VALUE_STRING);
                    }
                }
                if (attemptParsing) {
                    deserialized = tree.traverse(jp.getCodec()).readValueAs(BalanceMarkerTimestamp.class);
                    // TODO: there is no validation against JSON schema constraints
                    // (min, max, enum, pattern...), this does not perform a strict JSON
                    // validation, which means the 'match' count may be higher than it should be.
                    match++;
                    log.log(Level.FINER, "Input data matches schema 'BalanceMarkerTimestamp'");
                }
            } catch (Exception e) {
                // deserialization failed, continue
                log.log(Level.FINER, "Input data does not match schema 'BalanceMarkerTimestamp'", e);
            }

            // deserialize BalanceMarkerTransactionBlock
            try {
                boolean attemptParsing = true;
                // ensure that we respect type coercion as set on the client ObjectMapper
                if (BalanceMarkerTransactionBlock.class.equals(Integer.class) || BalanceMarkerTransactionBlock.class.equals(Long.class) || BalanceMarkerTransactionBlock.class.equals(Float.class) || BalanceMarkerTransactionBlock.class.equals(Double.class) || BalanceMarkerTransactionBlock.class.equals(Boolean.class) || BalanceMarkerTransactionBlock.class.equals(String.class)) {
                    attemptParsing = typeCoercion;
                    if (!attemptParsing) {
                        attemptParsing |= ((BalanceMarkerTransactionBlock.class.equals(Integer.class) || BalanceMarkerTransactionBlock.class.equals(Long.class)) && token == JsonToken.VALUE_NUMBER_INT);
                        attemptParsing |= ((BalanceMarkerTransactionBlock.class.equals(Float.class) || BalanceMarkerTransactionBlock.class.equals(Double.class)) && token == JsonToken.VALUE_NUMBER_FLOAT);
                        attemptParsing |= (BalanceMarkerTransactionBlock.class.equals(Boolean.class) && (token == JsonToken.VALUE_FALSE || token == JsonToken.VALUE_TRUE));
                        attemptParsing |= (BalanceMarkerTransactionBlock.class.equals(String.class) && token == JsonToken.VALUE_STRING);
                    }
                }
                if (attemptParsing) {
                    deserialized = tree.traverse(jp.getCodec()).readValueAs(BalanceMarkerTransactionBlock.class);
                    // TODO: there is no validation against JSON schema constraints
                    // (min, max, enum, pattern...), this does not perform a strict JSON
                    // validation, which means the 'match' count may be higher than it should be.
                    match++;
                    log.log(Level.FINER, "Input data matches schema 'BalanceMarkerTransactionBlock'");
                }
            } catch (Exception e) {
                // deserialization failed, continue
                log.log(Level.FINER, "Input data does not match schema 'BalanceMarkerTransactionBlock'", e);
            }

            if (match == 1) {
                BalanceMarker ret = new BalanceMarker();
                ret.setActualInstance(deserialized);
                return ret;
            }
            throw new IOException(String.format(Locale.ROOT, "Failed deserialization for BalanceMarker: %d classes match result, expected 1", match));
        }

        /**
         * Handle deserialization of the 'null' value.
         */
        @Override
        public BalanceMarker getNullValue(DeserializationContext ctxt) throws JsonMappingException {
            throw new JsonMappingException(ctxt.getParser(), "BalanceMarker cannot be null");
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<>();

    public BalanceMarker() {
        super("oneOf", Boolean.FALSE);
    }

    public BalanceMarker(BalanceMarkerTimestamp o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    public BalanceMarker(BalanceMarkerTransactionBlock o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("BalanceMarkerTimestamp", BalanceMarkerTimestamp.class);
        schemas.put("BalanceMarkerTransactionBlock", BalanceMarkerTransactionBlock.class);
        JSON.registerDescendants(BalanceMarker.class, Collections.unmodifiableMap(schemas));
        // Initialize and register the discriminator mappings.
        Map<String, Class<?>> mappings = new HashMap<String, Class<?>>();
        mappings.put("timestamp", BalanceMarkerTimestamp.class);
        mappings.put("transactionBlock", BalanceMarkerTransactionBlock.class);
        mappings.put("balanceMarker", BalanceMarker.class);
        JSON.registerDiscriminator(BalanceMarker.class, "type", mappings);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return BalanceMarker.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * BalanceMarkerTimestamp, BalanceMarkerTransactionBlock
     *
     * It could be an instance of the 'oneOf' schemas.
     * The oneOf child schemas may themselves be a composed schema (allOf, anyOf, oneOf).
     */
    @Override
    public void setActualInstance(Object instance) {
        if (JSON.isInstanceOf(BalanceMarkerTimestamp.class, instance, new HashSet<Class<?>>())) {
            super.setActualInstance(instance);
            return;
        }

        if (JSON.isInstanceOf(BalanceMarkerTransactionBlock.class, instance, new HashSet<Class<?>>())) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be BalanceMarkerTimestamp, BalanceMarkerTransactionBlock");
    }

    /**
     * Get the actual instance, which can be the following:
     * BalanceMarkerTimestamp, BalanceMarkerTransactionBlock
     *
     * @return The actual instance (BalanceMarkerTimestamp, BalanceMarkerTransactionBlock)
     */
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `BalanceMarkerTimestamp`. If the actual instance is not `BalanceMarkerTimestamp`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `BalanceMarkerTimestamp`
     * @throws ClassCastException if the instance is not `BalanceMarkerTimestamp`
     */
    public BalanceMarkerTimestamp getBalanceMarkerTimestamp() throws ClassCastException {
        return (BalanceMarkerTimestamp)super.getActualInstance();
    }

    /**
     * Get the actual instance of `BalanceMarkerTransactionBlock`. If the actual instance is not `BalanceMarkerTransactionBlock`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `BalanceMarkerTransactionBlock`
     * @throws ClassCastException if the instance is not `BalanceMarkerTransactionBlock`
     */
    public BalanceMarkerTransactionBlock getBalanceMarkerTransactionBlock() throws ClassCastException {
        return (BalanceMarkerTransactionBlock)super.getActualInstance();
    }



  /**
   * Convert the instance into URL query string.
   *
   * @return URL query string
   */
  public String toUrlQueryString() {
    return toUrlQueryString(null);
  }

  /**
   * Convert the instance into URL query string.
   *
   * @param prefix prefix of the query string
   * @return URL query string
   */
  public String toUrlQueryString(String prefix) {
    String suffix = "";
    String containerSuffix = "";
    String containerPrefix = "";
    if (prefix == null) {
      // style=form, explode=true, e.g. /pet?name=cat&type=manx
      prefix = "";
    } else {
      // deepObject style e.g. /pet?id[name]=cat&id[type]=manx
      prefix = prefix + "[";
      suffix = "]";
      containerSuffix = "]";
      containerPrefix = "[";
    }

    StringJoiner joiner = new StringJoiner("&");

    if (getActualInstance() instanceof BalanceMarkerTimestamp) {
        if (getActualInstance() != null) {
          joiner.add(((BalanceMarkerTimestamp)getActualInstance()).toUrlQueryString(prefix + "one_of_0" + suffix));
        }
        return joiner.toString();
    }
    if (getActualInstance() instanceof BalanceMarkerTransactionBlock) {
        if (getActualInstance() != null) {
          joiner.add(((BalanceMarkerTransactionBlock)getActualInstance()).toUrlQueryString(prefix + "one_of_1" + suffix));
        }
        return joiner.toString();
    }
    return null;
  }

}

