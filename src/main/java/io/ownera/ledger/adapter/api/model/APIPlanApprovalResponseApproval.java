/*
 * Ledger Adapter Specification
 * This is the API specification for the Ledger Adapter with whom the FinP2P Router will interact in order to execute and query the underlying implementation.
 *
 * The version of the OpenAPI document: x.x.x
 * Contact: support@ownera.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package io.ownera.ledger.adapter.api.model;

import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.StringJoiner;
import java.util.Objects;
import java.util.Map;
import java.util.HashMap;
import java.util.Locale;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonValue;
import io.ownera.ledger.adapter.api.model.APIPlanApproved;
import io.ownera.ledger.adapter.api.model.APIPlanRejected;
import io.ownera.ledger.adapter.api.model.APIPlanRejectedFailure;
import java.util.Arrays;
import com.fasterxml.jackson.annotation.JsonPropertyOrder;

import com.fasterxml.jackson.core.type.TypeReference;

import java.io.IOException;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.Locale;

import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.JsonToken;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonMappingException;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.MapperFeature;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import io.ownera.ledger.adapter.api.ApiClient;
import io.ownera.ledger.adapter.api.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2025-10-28T10:24:19.848612+02:00[Asia/Jerusalem]", comments = "Generator version: 7.16.0")
@JsonDeserialize(using = APIPlanApprovalResponseApproval.APIPlanApprovalResponseApprovalDeserializer.class)
@JsonSerialize(using = APIPlanApprovalResponseApproval.APIPlanApprovalResponseApprovalSerializer.class)
public class APIPlanApprovalResponseApproval extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(APIPlanApprovalResponseApproval.class.getName());

    public static class APIPlanApprovalResponseApprovalSerializer extends StdSerializer<APIPlanApprovalResponseApproval> {
        public APIPlanApprovalResponseApprovalSerializer(Class<APIPlanApprovalResponseApproval> t) {
            super(t);
        }

        public APIPlanApprovalResponseApprovalSerializer() {
            this(null);
        }

        @Override
        public void serialize(APIPlanApprovalResponseApproval value, JsonGenerator jgen, SerializerProvider provider) throws IOException, JsonProcessingException {
            jgen.writeObject(value.getActualInstance());
        }
    }

    public static class APIPlanApprovalResponseApprovalDeserializer extends StdDeserializer<APIPlanApprovalResponseApproval> {
        public APIPlanApprovalResponseApprovalDeserializer() {
            this(APIPlanApprovalResponseApproval.class);
        }

        public APIPlanApprovalResponseApprovalDeserializer(Class<?> vc) {
            super(vc);
        }

        @Override
        public APIPlanApprovalResponseApproval deserialize(JsonParser jp, DeserializationContext ctxt) throws IOException, JsonProcessingException {
            JsonNode tree = jp.readValueAsTree();
            Object deserialized = null;
            boolean typeCoercion = ctxt.isEnabled(MapperFeature.ALLOW_COERCION_OF_SCALARS);
            int match = 0;
            JsonToken token = tree.traverse(jp.getCodec()).nextToken();
            // deserialize APIPlanApproved
            try {
                boolean attemptParsing = true;
                // ensure that we respect type coercion as set on the client ObjectMapper
                if (APIPlanApproved.class.equals(Integer.class) || APIPlanApproved.class.equals(Long.class) || APIPlanApproved.class.equals(Float.class) || APIPlanApproved.class.equals(Double.class) || APIPlanApproved.class.equals(Boolean.class) || APIPlanApproved.class.equals(String.class)) {
                    attemptParsing = typeCoercion;
                    if (!attemptParsing) {
                        attemptParsing |= ((APIPlanApproved.class.equals(Integer.class) || APIPlanApproved.class.equals(Long.class)) && token == JsonToken.VALUE_NUMBER_INT);
                        attemptParsing |= ((APIPlanApproved.class.equals(Float.class) || APIPlanApproved.class.equals(Double.class)) && token == JsonToken.VALUE_NUMBER_FLOAT);
                        attemptParsing |= (APIPlanApproved.class.equals(Boolean.class) && (token == JsonToken.VALUE_FALSE || token == JsonToken.VALUE_TRUE));
                        attemptParsing |= (APIPlanApproved.class.equals(String.class) && token == JsonToken.VALUE_STRING);
                    }
                }
                if (attemptParsing) {
                    deserialized = tree.traverse(jp.getCodec()).readValueAs(APIPlanApproved.class);
                    // TODO: there is no validation against JSON schema constraints
                    // (min, max, enum, pattern...), this does not perform a strict JSON
                    // validation, which means the 'match' count may be higher than it should be.
                    match++;
                    log.log(Level.FINER, "Input data matches schema 'APIPlanApproved'");
                }
            } catch (Exception e) {
                // deserialization failed, continue
                log.log(Level.FINER, "Input data does not match schema 'APIPlanApproved'", e);
            }

            // deserialize APIPlanRejected
            try {
                boolean attemptParsing = true;
                // ensure that we respect type coercion as set on the client ObjectMapper
                if (APIPlanRejected.class.equals(Integer.class) || APIPlanRejected.class.equals(Long.class) || APIPlanRejected.class.equals(Float.class) || APIPlanRejected.class.equals(Double.class) || APIPlanRejected.class.equals(Boolean.class) || APIPlanRejected.class.equals(String.class)) {
                    attemptParsing = typeCoercion;
                    if (!attemptParsing) {
                        attemptParsing |= ((APIPlanRejected.class.equals(Integer.class) || APIPlanRejected.class.equals(Long.class)) && token == JsonToken.VALUE_NUMBER_INT);
                        attemptParsing |= ((APIPlanRejected.class.equals(Float.class) || APIPlanRejected.class.equals(Double.class)) && token == JsonToken.VALUE_NUMBER_FLOAT);
                        attemptParsing |= (APIPlanRejected.class.equals(Boolean.class) && (token == JsonToken.VALUE_FALSE || token == JsonToken.VALUE_TRUE));
                        attemptParsing |= (APIPlanRejected.class.equals(String.class) && token == JsonToken.VALUE_STRING);
                    }
                }
                if (attemptParsing) {
                    deserialized = tree.traverse(jp.getCodec()).readValueAs(APIPlanRejected.class);
                    // TODO: there is no validation against JSON schema constraints
                    // (min, max, enum, pattern...), this does not perform a strict JSON
                    // validation, which means the 'match' count may be higher than it should be.
                    match++;
                    log.log(Level.FINER, "Input data matches schema 'APIPlanRejected'");
                }
            } catch (Exception e) {
                // deserialization failed, continue
                log.log(Level.FINER, "Input data does not match schema 'APIPlanRejected'", e);
            }

            if (match == 1) {
                APIPlanApprovalResponseApproval ret = new APIPlanApprovalResponseApproval();
                ret.setActualInstance(deserialized);
                return ret;
            }
            throw new IOException(String.format(Locale.ROOT, "Failed deserialization for APIPlanApprovalResponseApproval: %d classes match result, expected 1", match));
        }

        /**
         * Handle deserialization of the 'null' value.
         */
        @Override
        public APIPlanApprovalResponseApproval getNullValue(DeserializationContext ctxt) throws JsonMappingException {
            throw new JsonMappingException(ctxt.getParser(), "APIPlanApprovalResponseApproval cannot be null");
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<>();

    public APIPlanApprovalResponseApproval() {
        super("oneOf", Boolean.FALSE);
    }

    public APIPlanApprovalResponseApproval(APIPlanApproved o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    public APIPlanApprovalResponseApproval(APIPlanRejected o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("APIPlanApproved", APIPlanApproved.class);
        schemas.put("APIPlanRejected", APIPlanRejected.class);
        JSON.registerDescendants(APIPlanApprovalResponseApproval.class, Collections.unmodifiableMap(schemas));
        // Initialize and register the discriminator mappings.
        Map<String, Class<?>> mappings = new HashMap<String, Class<?>>();
        mappings.put("approved", APIPlanApproved.class);
        mappings.put("rejected", APIPlanRejected.class);
        mappings.put("PlanApprovalResponse_approval", APIPlanApprovalResponseApproval.class);
        JSON.registerDiscriminator(APIPlanApprovalResponseApproval.class, "status", mappings);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return APIPlanApprovalResponseApproval.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * APIPlanApproved, APIPlanRejected
     *
     * It could be an instance of the 'oneOf' schemas.
     * The oneOf child schemas may themselves be a composed schema (allOf, anyOf, oneOf).
     */
    @Override
    public void setActualInstance(Object instance) {
        if (JSON.isInstanceOf(APIPlanApproved.class, instance, new HashSet<Class<?>>())) {
            super.setActualInstance(instance);
            return;
        }

        if (JSON.isInstanceOf(APIPlanRejected.class, instance, new HashSet<Class<?>>())) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be APIPlanApproved, APIPlanRejected");
    }

    /**
     * Get the actual instance, which can be the following:
     * APIPlanApproved, APIPlanRejected
     *
     * @return The actual instance (APIPlanApproved, APIPlanRejected)
     */
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `APIPlanApproved`. If the actual instance is not `APIPlanApproved`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `APIPlanApproved`
     * @throws ClassCastException if the instance is not `APIPlanApproved`
     */
    public APIPlanApproved getAPIPlanApproved() throws ClassCastException {
        return (APIPlanApproved)super.getActualInstance();
    }

    /**
     * Get the actual instance of `APIPlanRejected`. If the actual instance is not `APIPlanRejected`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `APIPlanRejected`
     * @throws ClassCastException if the instance is not `APIPlanRejected`
     */
    public APIPlanRejected getAPIPlanRejected() throws ClassCastException {
        return (APIPlanRejected)super.getActualInstance();
    }



  /**
   * Convert the instance into URL query string.
   *
   * @return URL query string
   */
  public String toUrlQueryString() {
    return toUrlQueryString(null);
  }

  /**
   * Convert the instance into URL query string.
   *
   * @param prefix prefix of the query string
   * @return URL query string
   */
  public String toUrlQueryString(String prefix) {
    String suffix = "";
    String containerSuffix = "";
    String containerPrefix = "";
    if (prefix == null) {
      // style=form, explode=true, e.g. /pet?name=cat&type=manx
      prefix = "";
    } else {
      // deepObject style e.g. /pet?id[name]=cat&id[type]=manx
      prefix = prefix + "[";
      suffix = "]";
      containerSuffix = "]";
      containerPrefix = "[";
    }

    StringJoiner joiner = new StringJoiner("&");

    if (getActualInstance() instanceof APIPlanApproved) {
        if (getActualInstance() != null) {
          joiner.add(((APIPlanApproved)getActualInstance()).toUrlQueryString(prefix + "one_of_0" + suffix));
        }
        return joiner.toString();
    }
    if (getActualInstance() instanceof APIPlanRejected) {
        if (getActualInstance() != null) {
          joiner.add(((APIPlanRejected)getActualInstance()).toUrlQueryString(prefix + "one_of_1" + suffix));
        }
        return joiner.toString();
    }
    return null;
  }

}

