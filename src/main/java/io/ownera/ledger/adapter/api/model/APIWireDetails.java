/*
 * Ledger Adapter Specification
 * This is the API specification for the Ledger Adapter with whom the FinP2P Router will interact in order to execute and query the underlying implementation.
 *
 * The version of the OpenAPI document: x.x.x
 * Contact: support@ownera.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package io.ownera.ledger.adapter.api.model;

import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.StringJoiner;
import java.util.Objects;
import java.util.Map;
import java.util.HashMap;
import java.util.Locale;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonValue;
import io.ownera.ledger.adapter.api.model.APIIbanAccountDetails;
import io.ownera.ledger.adapter.api.model.APISortCodeDetails;
import io.ownera.ledger.adapter.api.model.APISwiftAccountDetails;
import java.util.Arrays;
import com.fasterxml.jackson.annotation.JsonPropertyOrder;

import com.fasterxml.jackson.core.type.TypeReference;

import java.io.IOException;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.Locale;

import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.JsonToken;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonMappingException;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.MapperFeature;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import io.ownera.ledger.adapter.api.ApiClient;
import io.ownera.ledger.adapter.api.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2025-10-28T10:24:19.848612+02:00[Asia/Jerusalem]", comments = "Generator version: 7.16.0")
@JsonDeserialize(using = APIWireDetails.APIWireDetailsDeserializer.class)
@JsonSerialize(using = APIWireDetails.APIWireDetailsSerializer.class)
public class APIWireDetails extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(APIWireDetails.class.getName());

    public static class APIWireDetailsSerializer extends StdSerializer<APIWireDetails> {
        public APIWireDetailsSerializer(Class<APIWireDetails> t) {
            super(t);
        }

        public APIWireDetailsSerializer() {
            this(null);
        }

        @Override
        public void serialize(APIWireDetails value, JsonGenerator jgen, SerializerProvider provider) throws IOException, JsonProcessingException {
            jgen.writeObject(value.getActualInstance());
        }
    }

    public static class APIWireDetailsDeserializer extends StdDeserializer<APIWireDetails> {
        public APIWireDetailsDeserializer() {
            this(APIWireDetails.class);
        }

        public APIWireDetailsDeserializer(Class<?> vc) {
            super(vc);
        }

        @Override
        public APIWireDetails deserialize(JsonParser jp, DeserializationContext ctxt) throws IOException, JsonProcessingException {
            JsonNode tree = jp.readValueAsTree();
            Object deserialized = null;
            boolean typeCoercion = ctxt.isEnabled(MapperFeature.ALLOW_COERCION_OF_SCALARS);
            int match = 0;
            JsonToken token = tree.traverse(jp.getCodec()).nextToken();
            // deserialize APIIbanAccountDetails
            try {
                boolean attemptParsing = true;
                // ensure that we respect type coercion as set on the client ObjectMapper
                if (APIIbanAccountDetails.class.equals(Integer.class) || APIIbanAccountDetails.class.equals(Long.class) || APIIbanAccountDetails.class.equals(Float.class) || APIIbanAccountDetails.class.equals(Double.class) || APIIbanAccountDetails.class.equals(Boolean.class) || APIIbanAccountDetails.class.equals(String.class)) {
                    attemptParsing = typeCoercion;
                    if (!attemptParsing) {
                        attemptParsing |= ((APIIbanAccountDetails.class.equals(Integer.class) || APIIbanAccountDetails.class.equals(Long.class)) && token == JsonToken.VALUE_NUMBER_INT);
                        attemptParsing |= ((APIIbanAccountDetails.class.equals(Float.class) || APIIbanAccountDetails.class.equals(Double.class)) && token == JsonToken.VALUE_NUMBER_FLOAT);
                        attemptParsing |= (APIIbanAccountDetails.class.equals(Boolean.class) && (token == JsonToken.VALUE_FALSE || token == JsonToken.VALUE_TRUE));
                        attemptParsing |= (APIIbanAccountDetails.class.equals(String.class) && token == JsonToken.VALUE_STRING);
                    }
                }
                if (attemptParsing) {
                    deserialized = tree.traverse(jp.getCodec()).readValueAs(APIIbanAccountDetails.class);
                    // TODO: there is no validation against JSON schema constraints
                    // (min, max, enum, pattern...), this does not perform a strict JSON
                    // validation, which means the 'match' count may be higher than it should be.
                    match++;
                    log.log(Level.FINER, "Input data matches schema 'APIIbanAccountDetails'");
                }
            } catch (Exception e) {
                // deserialization failed, continue
                log.log(Level.FINER, "Input data does not match schema 'APIIbanAccountDetails'", e);
            }

            // deserialize APISortCodeDetails
            try {
                boolean attemptParsing = true;
                // ensure that we respect type coercion as set on the client ObjectMapper
                if (APISortCodeDetails.class.equals(Integer.class) || APISortCodeDetails.class.equals(Long.class) || APISortCodeDetails.class.equals(Float.class) || APISortCodeDetails.class.equals(Double.class) || APISortCodeDetails.class.equals(Boolean.class) || APISortCodeDetails.class.equals(String.class)) {
                    attemptParsing = typeCoercion;
                    if (!attemptParsing) {
                        attemptParsing |= ((APISortCodeDetails.class.equals(Integer.class) || APISortCodeDetails.class.equals(Long.class)) && token == JsonToken.VALUE_NUMBER_INT);
                        attemptParsing |= ((APISortCodeDetails.class.equals(Float.class) || APISortCodeDetails.class.equals(Double.class)) && token == JsonToken.VALUE_NUMBER_FLOAT);
                        attemptParsing |= (APISortCodeDetails.class.equals(Boolean.class) && (token == JsonToken.VALUE_FALSE || token == JsonToken.VALUE_TRUE));
                        attemptParsing |= (APISortCodeDetails.class.equals(String.class) && token == JsonToken.VALUE_STRING);
                    }
                }
                if (attemptParsing) {
                    deserialized = tree.traverse(jp.getCodec()).readValueAs(APISortCodeDetails.class);
                    // TODO: there is no validation against JSON schema constraints
                    // (min, max, enum, pattern...), this does not perform a strict JSON
                    // validation, which means the 'match' count may be higher than it should be.
                    match++;
                    log.log(Level.FINER, "Input data matches schema 'APISortCodeDetails'");
                }
            } catch (Exception e) {
                // deserialization failed, continue
                log.log(Level.FINER, "Input data does not match schema 'APISortCodeDetails'", e);
            }

            // deserialize APISwiftAccountDetails
            try {
                boolean attemptParsing = true;
                // ensure that we respect type coercion as set on the client ObjectMapper
                if (APISwiftAccountDetails.class.equals(Integer.class) || APISwiftAccountDetails.class.equals(Long.class) || APISwiftAccountDetails.class.equals(Float.class) || APISwiftAccountDetails.class.equals(Double.class) || APISwiftAccountDetails.class.equals(Boolean.class) || APISwiftAccountDetails.class.equals(String.class)) {
                    attemptParsing = typeCoercion;
                    if (!attemptParsing) {
                        attemptParsing |= ((APISwiftAccountDetails.class.equals(Integer.class) || APISwiftAccountDetails.class.equals(Long.class)) && token == JsonToken.VALUE_NUMBER_INT);
                        attemptParsing |= ((APISwiftAccountDetails.class.equals(Float.class) || APISwiftAccountDetails.class.equals(Double.class)) && token == JsonToken.VALUE_NUMBER_FLOAT);
                        attemptParsing |= (APISwiftAccountDetails.class.equals(Boolean.class) && (token == JsonToken.VALUE_FALSE || token == JsonToken.VALUE_TRUE));
                        attemptParsing |= (APISwiftAccountDetails.class.equals(String.class) && token == JsonToken.VALUE_STRING);
                    }
                }
                if (attemptParsing) {
                    deserialized = tree.traverse(jp.getCodec()).readValueAs(APISwiftAccountDetails.class);
                    // TODO: there is no validation against JSON schema constraints
                    // (min, max, enum, pattern...), this does not perform a strict JSON
                    // validation, which means the 'match' count may be higher than it should be.
                    match++;
                    log.log(Level.FINER, "Input data matches schema 'APISwiftAccountDetails'");
                }
            } catch (Exception e) {
                // deserialization failed, continue
                log.log(Level.FINER, "Input data does not match schema 'APISwiftAccountDetails'", e);
            }

            if (match == 1) {
                APIWireDetails ret = new APIWireDetails();
                ret.setActualInstance(deserialized);
                return ret;
            }
            throw new IOException(String.format(Locale.ROOT, "Failed deserialization for APIWireDetails: %d classes match result, expected 1", match));
        }

        /**
         * Handle deserialization of the 'null' value.
         */
        @Override
        public APIWireDetails getNullValue(DeserializationContext ctxt) throws JsonMappingException {
            throw new JsonMappingException(ctxt.getParser(), "APIWireDetails cannot be null");
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<>();

    public APIWireDetails() {
        super("oneOf", Boolean.FALSE);
    }

    public APIWireDetails(APIIbanAccountDetails o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    public APIWireDetails(APISortCodeDetails o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    public APIWireDetails(APISwiftAccountDetails o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("APIIbanAccountDetails", APIIbanAccountDetails.class);
        schemas.put("APISortCodeDetails", APISortCodeDetails.class);
        schemas.put("APISwiftAccountDetails", APISwiftAccountDetails.class);
        JSON.registerDescendants(APIWireDetails.class, Collections.unmodifiableMap(schemas));
        // Initialize and register the discriminator mappings.
        Map<String, Class<?>> mappings = new HashMap<String, Class<?>>();
        mappings.put("iban", APIIbanAccountDetails.class);
        mappings.put("sortCode", APISortCodeDetails.class);
        mappings.put("swift", APISwiftAccountDetails.class);
        mappings.put("wireDetails", APIWireDetails.class);
        JSON.registerDiscriminator(APIWireDetails.class, "type", mappings);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return APIWireDetails.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * APIIbanAccountDetails, APISortCodeDetails, APISwiftAccountDetails
     *
     * It could be an instance of the 'oneOf' schemas.
     * The oneOf child schemas may themselves be a composed schema (allOf, anyOf, oneOf).
     */
    @Override
    public void setActualInstance(Object instance) {
        if (JSON.isInstanceOf(APIIbanAccountDetails.class, instance, new HashSet<Class<?>>())) {
            super.setActualInstance(instance);
            return;
        }

        if (JSON.isInstanceOf(APISortCodeDetails.class, instance, new HashSet<Class<?>>())) {
            super.setActualInstance(instance);
            return;
        }

        if (JSON.isInstanceOf(APISwiftAccountDetails.class, instance, new HashSet<Class<?>>())) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be APIIbanAccountDetails, APISortCodeDetails, APISwiftAccountDetails");
    }

    /**
     * Get the actual instance, which can be the following:
     * APIIbanAccountDetails, APISortCodeDetails, APISwiftAccountDetails
     *
     * @return The actual instance (APIIbanAccountDetails, APISortCodeDetails, APISwiftAccountDetails)
     */
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `APIIbanAccountDetails`. If the actual instance is not `APIIbanAccountDetails`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `APIIbanAccountDetails`
     * @throws ClassCastException if the instance is not `APIIbanAccountDetails`
     */
    public APIIbanAccountDetails getAPIIbanAccountDetails() throws ClassCastException {
        return (APIIbanAccountDetails)super.getActualInstance();
    }

    /**
     * Get the actual instance of `APISortCodeDetails`. If the actual instance is not `APISortCodeDetails`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `APISortCodeDetails`
     * @throws ClassCastException if the instance is not `APISortCodeDetails`
     */
    public APISortCodeDetails getAPISortCodeDetails() throws ClassCastException {
        return (APISortCodeDetails)super.getActualInstance();
    }

    /**
     * Get the actual instance of `APISwiftAccountDetails`. If the actual instance is not `APISwiftAccountDetails`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `APISwiftAccountDetails`
     * @throws ClassCastException if the instance is not `APISwiftAccountDetails`
     */
    public APISwiftAccountDetails getAPISwiftAccountDetails() throws ClassCastException {
        return (APISwiftAccountDetails)super.getActualInstance();
    }



  /**
   * Convert the instance into URL query string.
   *
   * @return URL query string
   */
  public String toUrlQueryString() {
    return toUrlQueryString(null);
  }

  /**
   * Convert the instance into URL query string.
   *
   * @param prefix prefix of the query string
   * @return URL query string
   */
  public String toUrlQueryString(String prefix) {
    String suffix = "";
    String containerSuffix = "";
    String containerPrefix = "";
    if (prefix == null) {
      // style=form, explode=true, e.g. /pet?name=cat&type=manx
      prefix = "";
    } else {
      // deepObject style e.g. /pet?id[name]=cat&id[type]=manx
      prefix = prefix + "[";
      suffix = "]";
      containerSuffix = "]";
      containerPrefix = "[";
    }

    StringJoiner joiner = new StringJoiner("&");

    if (getActualInstance() instanceof APIIbanAccountDetails) {
        if (getActualInstance() != null) {
          joiner.add(((APIIbanAccountDetails)getActualInstance()).toUrlQueryString(prefix + "one_of_0" + suffix));
        }
        return joiner.toString();
    }
    if (getActualInstance() instanceof APISwiftAccountDetails) {
        if (getActualInstance() != null) {
          joiner.add(((APISwiftAccountDetails)getActualInstance()).toUrlQueryString(prefix + "one_of_1" + suffix));
        }
        return joiner.toString();
    }
    if (getActualInstance() instanceof APISortCodeDetails) {
        if (getActualInstance() != null) {
          joiner.add(((APISortCodeDetails)getActualInstance()).toUrlQueryString(prefix + "one_of_2" + suffix));
        }
        return joiner.toString();
    }
    return null;
  }

}

