/*
 * Ledger Adapter Specification
 * This is the API specification for the Ledger Adapter with whom the FinP2P node will interact in order to execute and query the underlying implementation.
 *
 * The version of the OpenAPI document: x.x.x
 * Contact: support@ownera.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package io.ownera.ledger.adapter.api.model;

import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.StringJoiner;
import java.util.Objects;
import java.util.Map;
import java.util.HashMap;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonValue;
import io.ownera.ledger.adapter.api.model.EIP712Domain;
import io.ownera.ledger.adapter.api.model.EIP712Template;
import io.ownera.ledger.adapter.api.model.EIP712TypedValue;
import io.ownera.ledger.adapter.api.model.EIP712Types;
import io.ownera.ledger.adapter.api.model.HashGroup;
import io.ownera.ledger.adapter.api.model.HashListTemplate;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.HashMap;
import java.util.List;
import java.util.Map;
import com.fasterxml.jackson.annotation.JsonPropertyOrder;

import com.fasterxml.jackson.core.type.TypeReference;

import java.io.IOException;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;

import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.JsonToken;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonMappingException;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.MapperFeature;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import io.ownera.ledger.adapter.api.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2024-10-23T11:29:49.092442+03:00[Asia/Jerusalem]", comments = "Generator version: 7.9.0")
@JsonDeserialize(using = SignatureTemplate.SignatureTemplateDeserializer.class)
@JsonSerialize(using = SignatureTemplate.SignatureTemplateSerializer.class)
public class SignatureTemplate extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(SignatureTemplate.class.getName());

    public static class SignatureTemplateSerializer extends StdSerializer<SignatureTemplate> {
        public SignatureTemplateSerializer(Class<SignatureTemplate> t) {
            super(t);
        }

        public SignatureTemplateSerializer() {
            this(null);
        }

        @Override
        public void serialize(SignatureTemplate value, JsonGenerator jgen, SerializerProvider provider) throws IOException, JsonProcessingException {
            jgen.writeObject(value.getActualInstance());
        }
    }

    public static class SignatureTemplateDeserializer extends StdDeserializer<SignatureTemplate> {
        public SignatureTemplateDeserializer() {
            this(SignatureTemplate.class);
        }

        public SignatureTemplateDeserializer(Class<?> vc) {
            super(vc);
        }

        @Override
        public SignatureTemplate deserialize(JsonParser jp, DeserializationContext ctxt) throws IOException, JsonProcessingException {
            JsonNode tree = jp.readValueAsTree();
            Object deserialized = null;
            boolean typeCoercion = ctxt.isEnabled(MapperFeature.ALLOW_COERCION_OF_SCALARS);
            int match = 0;
            JsonToken token = tree.traverse(jp.getCodec()).nextToken();
            // deserialize EIP712Template
            try {
                boolean attemptParsing = true;
                // ensure that we respect type coercion as set on the client ObjectMapper
                if (EIP712Template.class.equals(Integer.class) || EIP712Template.class.equals(Long.class) || EIP712Template.class.equals(Float.class) || EIP712Template.class.equals(Double.class) || EIP712Template.class.equals(Boolean.class) || EIP712Template.class.equals(String.class)) {
                    attemptParsing = typeCoercion;
                    if (!attemptParsing) {
                        attemptParsing |= ((EIP712Template.class.equals(Integer.class) || EIP712Template.class.equals(Long.class)) && token == JsonToken.VALUE_NUMBER_INT);
                        attemptParsing |= ((EIP712Template.class.equals(Float.class) || EIP712Template.class.equals(Double.class)) && token == JsonToken.VALUE_NUMBER_FLOAT);
                        attemptParsing |= (EIP712Template.class.equals(Boolean.class) && (token == JsonToken.VALUE_FALSE || token == JsonToken.VALUE_TRUE));
                        attemptParsing |= (EIP712Template.class.equals(String.class) && token == JsonToken.VALUE_STRING);
                    }
                }
                if (attemptParsing) {
                    deserialized = tree.traverse(jp.getCodec()).readValueAs(EIP712Template.class);
                    // TODO: there is no validation against JSON schema constraints
                    // (min, max, enum, pattern...), this does not perform a strict JSON
                    // validation, which means the 'match' count may be higher than it should be.
                    match++;
                    log.log(Level.FINER, "Input data matches schema 'EIP712Template'");
                }
            } catch (Exception e) {
                // deserialization failed, continue
                log.log(Level.FINER, "Input data does not match schema 'EIP712Template'", e);
            }

            // deserialize HashListTemplate
            try {
                boolean attemptParsing = true;
                // ensure that we respect type coercion as set on the client ObjectMapper
                if (HashListTemplate.class.equals(Integer.class) || HashListTemplate.class.equals(Long.class) || HashListTemplate.class.equals(Float.class) || HashListTemplate.class.equals(Double.class) || HashListTemplate.class.equals(Boolean.class) || HashListTemplate.class.equals(String.class)) {
                    attemptParsing = typeCoercion;
                    if (!attemptParsing) {
                        attemptParsing |= ((HashListTemplate.class.equals(Integer.class) || HashListTemplate.class.equals(Long.class)) && token == JsonToken.VALUE_NUMBER_INT);
                        attemptParsing |= ((HashListTemplate.class.equals(Float.class) || HashListTemplate.class.equals(Double.class)) && token == JsonToken.VALUE_NUMBER_FLOAT);
                        attemptParsing |= (HashListTemplate.class.equals(Boolean.class) && (token == JsonToken.VALUE_FALSE || token == JsonToken.VALUE_TRUE));
                        attemptParsing |= (HashListTemplate.class.equals(String.class) && token == JsonToken.VALUE_STRING);
                    }
                }
                if (attemptParsing) {
                    deserialized = tree.traverse(jp.getCodec()).readValueAs(HashListTemplate.class);
                    // TODO: there is no validation against JSON schema constraints
                    // (min, max, enum, pattern...), this does not perform a strict JSON
                    // validation, which means the 'match' count may be higher than it should be.
                    match++;
                    log.log(Level.FINER, "Input data matches schema 'HashListTemplate'");
                }
            } catch (Exception e) {
                // deserialization failed, continue
                log.log(Level.FINER, "Input data does not match schema 'HashListTemplate'", e);
            }

            if (match == 1) {
                SignatureTemplate ret = new SignatureTemplate();
                ret.setActualInstance(deserialized);
                return ret;
            }
            throw new IOException(String.format("Failed deserialization for SignatureTemplate: %d classes match result, expected 1", match));
        }

        /**
         * Handle deserialization of the 'null' value.
         */
        @Override
        public SignatureTemplate getNullValue(DeserializationContext ctxt) throws JsonMappingException {
            throw new JsonMappingException(ctxt.getParser(), "SignatureTemplate cannot be null");
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<>();

    public SignatureTemplate() {
        super("oneOf", Boolean.FALSE);
    }

    public SignatureTemplate(EIP712Template o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    public SignatureTemplate(HashListTemplate o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("EIP712Template", EIP712Template.class);
        schemas.put("HashListTemplate", HashListTemplate.class);
        JSON.registerDescendants(SignatureTemplate.class, Collections.unmodifiableMap(schemas));
        // Initialize and register the discriminator mappings.
        Map<String, Class<?>> mappings = new HashMap<String, Class<?>>();
        mappings.put("EIP712", EIP712Template.class);
        mappings.put("hashList", HashListTemplate.class);
        mappings.put("EIP712Template", EIP712Template.class);
        mappings.put("hashListTemplate", HashListTemplate.class);
        mappings.put("signatureTemplate", SignatureTemplate.class);
        JSON.registerDiscriminator(SignatureTemplate.class, "type", mappings);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return SignatureTemplate.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * EIP712Template, HashListTemplate
     *
     * It could be an instance of the 'oneOf' schemas.
     * The oneOf child schemas may themselves be a composed schema (allOf, anyOf, oneOf).
     */
    @Override
    public void setActualInstance(Object instance) {
        if (JSON.isInstanceOf(EIP712Template.class, instance, new HashSet<Class<?>>())) {
            super.setActualInstance(instance);
            return;
        }

        if (JSON.isInstanceOf(HashListTemplate.class, instance, new HashSet<Class<?>>())) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be EIP712Template, HashListTemplate");
    }

    /**
     * Get the actual instance, which can be the following:
     * EIP712Template, HashListTemplate
     *
     * @return The actual instance (EIP712Template, HashListTemplate)
     */
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `EIP712Template`. If the actual instance is not `EIP712Template`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `EIP712Template`
     * @throws ClassCastException if the instance is not `EIP712Template`
     */
    public EIP712Template getEIP712Template() throws ClassCastException {
        return (EIP712Template)super.getActualInstance();
    }

    /**
     * Get the actual instance of `HashListTemplate`. If the actual instance is not `HashListTemplate`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `HashListTemplate`
     * @throws ClassCastException if the instance is not `HashListTemplate`
     */
    public HashListTemplate getHashListTemplate() throws ClassCastException {
        return (HashListTemplate)super.getActualInstance();
    }



  /**
   * Convert the instance into URL query string.
   *
   * @return URL query string
   */
  public String toUrlQueryString() {
    return toUrlQueryString(null);
  }

  /**
   * Convert the instance into URL query string.
   *
   * @param prefix prefix of the query string
   * @return URL query string
   */
  public String toUrlQueryString(String prefix) {
    String suffix = "";
    String containerSuffix = "";
    String containerPrefix = "";
    if (prefix == null) {
      // style=form, explode=true, e.g. /pet?name=cat&type=manx
      prefix = "";
    } else {
      // deepObject style e.g. /pet?id[name]=cat&id[type]=manx
      prefix = prefix + "[";
      suffix = "]";
      containerSuffix = "]";
      containerPrefix = "[";
    }

    StringJoiner joiner = new StringJoiner("&");

    if (getActualInstance() instanceof HashListTemplate) {
        if (getActualInstance() != null) {
          joiner.add(((HashListTemplate)getActualInstance()).toUrlQueryString(prefix + "one_of_0" + suffix));
        }
        return joiner.toString();
    }
    if (getActualInstance() instanceof EIP712Template) {
        if (getActualInstance() != null) {
          joiner.add(((EIP712Template)getActualInstance()).toUrlQueryString(prefix + "one_of_1" + suffix));
        }
        return joiner.toString();
    }
    return null;
  }

}

