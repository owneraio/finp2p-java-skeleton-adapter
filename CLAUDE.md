# Claude Code Rules

## Git Workflow — STRICT
- NEVER merge PRs without explicit user approval
- NEVER create GitHub releases or tags without explicit user approval
- NEVER push to master/main directly
- Only create branches and PRs when asked — then STOP and wait for further instructions
- Version bumps require explicit user approval (version scheme: major.minor = FinP2P protocol version, patch = ours)
